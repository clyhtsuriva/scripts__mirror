---
- name: Update the server
  hosts: vps
  become: true
  tasks:

    - name: Update and upgrade apt packages
      ansible.builtin.apt:
        update_cache: true
        upgrade: true
        autoremove: true
...
